# 1

def verificar_transacciones(s: str) -> int:
  res: int = (350 * ap_antes_corte("r", s)) - (56 * ap_antes_corte("v", s))

  return res

def ap_antes_corte(c: str, s: str) -> int:
  apariciones: int = 0
  res: int = 0

  saldo: int = 0
  
  while saldo >= 0:
    
  
  return res

verificar_transacciones("ssrvvvvsvvsvvv")





